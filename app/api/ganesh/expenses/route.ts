import {NextResponse} from 'next/server';import {supabaseAdmin} from '@/lib/supabaseAdmin';export async function GET(){const {data,error}=await supabaseAdmin().from('ganesh_expenses').select('expense_date,category,description,amount,bill_url').order('expense_date',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({expenses:data||[],total:(data||[]).reduce((s,x)=>s+Number(x.amount),0)})}