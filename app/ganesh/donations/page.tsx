'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import {money} from '@/lib/money';
export default function Donations(){const [tab,setTab]=useState('donate'),[items,setItems]=useState<any[]>([]),[total,setTotal]=useState(0),[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/ganesh/donations');if(r.ok){const j=await r.json();setItems(j.donations||[]);setTotal(j.total||0)}}useEffect(()=>{load()},[]);async function submit(e:any){e.preventDefault();setMsg('Saving...');const f=new FormData(e.currentTarget);const r=await fetch('/api/ganesh/donations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f.entries()))});if(r.ok){e.currentTarget.reset();setMsg('Donation submitted for admin confirmation. Please complete payment using the Pay Now button.')}else{const j=await r.json();setMsg(j.error||'Could not submit')}}return <main className="wrap"><div className="header"><div><h1>🙏 Festival Donations</h1><p className="muted">Ganesh Chaturthi 2026</p></div><Link className="btn secondary" href="/ganesh">Back</Link></div><div className="card"><div className="section-tabs"><button className={tab==='donate'?'active':''} onClick={()=>setTab('donate')}>Donate</button><button className={tab==='donors'?'active':''} onClick={()=>setTab('donors')}>Donations Received</button></div>{tab==='donate'?<form className="grid" onSubmit={submit}><label>Flat Number</label><input name="flat_no" required placeholder="A1"/><label>Name</label><input name="donor_name" required/><label>Donation Amount</label><input name="amount" type="number" min="1" required/><button className="btn">Submit Donation</button><button className="btn secondary" type="button" onClick={()=>alert('PhonePe payment link will be added here.')}>Pay Now</button>{msg&&<p className="muted">{msg}</p>}</form>:<div><div className="stat green"><span>Total Confirmed Donations</span><br/><b>{money(total)}</b></div><p className="muted">Donor names and flat numbers are kept private.</p><div className="expense-category-list">{items.map((x,i)=><div className="expense-category-row" key={i}><span>Anonymous Donation</span><b>{money(Number(x.amount))}</b></div>)}</div></div>}</div></main>}
